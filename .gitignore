__pycache__/
*.pyc

# Runtime state files (per-DB slug variants included) and their temp/lock files
sync_map.json
sync_map_*.json
thread_meta.json
thread_meta_*.json
origin_map.json
origin_map_*.json
*.json.lock
*.json.tmp.*

# Cross-process watcher ownership lock (per-DB slug variants included)
watcher.lock
watcher_*.lock

# Local tool state (not part of the plugin)
.serena/
.tokensave/

# Test run logs
tests/test_run_*.log
